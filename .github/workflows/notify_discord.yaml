name: notify

on:
  pull_request:
    types: [opened, closed]

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Send message for pull request opened
        if: github.event_name == 'pull_request' && github.event.action == 'opened'
        uses: appleboy/discord-action@master
        with:
          webhook_url: ${{ secrets.DISCORD_WEBHOOK }}
          username: "GitHub Bot"
          avatar_url: "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png"
          message: |
            Olá!
            🎉 **Um novo Pull Request foi aberto no repositório ${{ github.event.repository.name }}!**

            **${{ github.event.pull_request.title }}**

            - **👤 Autor:** ${{ github.event.pull_request.user.login }}
            - **🔢 PR #${{ github.event.pull_request.number }}**
            - **🌿 Branch:** ${{ github.event.pull_request.head.ref }} → ${{ github.event.pull_request.base.ref }}

            - **🔗 [Ver Pull Request](${{ github.event.pull_request.html_url }})**

      - name: Send message for pull request merged
        if: github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true
        uses: appleboy/discord-action@master
        with:
          webhook_url: ${{ secrets.DISCORD_WEBHOOK }}
          username: "GitHub Bot"
          avatar_url: "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png"
          message: |
            Olá!
            ✅ **O Pull Request foi mergeado no repositório ${{ github.event.repository.name }}!**

            **${{ github.event.pull_request.title }}**

            - **👤 Autor:** ${{ github.event.pull_request.user.login }}
            - **🔢 PR #${{ github.event.pull_request.number }}**
            - **🌿 Branch:** ${{ github.event.pull_request.head.ref }} → ${{ github.event.pull_request.base.ref }}

            - **🔗 [Ver Pull Request](${{ github.event.pull_request.html_url }})**

